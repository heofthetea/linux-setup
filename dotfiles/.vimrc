" Enable line numbers
set number

" Set the tab width to 4 spaces
set tabstop=4
set shiftwidth=4
set expandtab
" Allow the cursor to move to the end of the line in Normal mode
set virtualedit=onemore

